{
  "name": "barclays-mern",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server",
    "server": "npm start --prefix server",
    "ml": "python ml_service/app.py",
    "dev": "concurrently \"npm run server\" \"npm run ml\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
